query ConfigQuery($workflowId: BigInt!) {
  account: me {
    userId: rowId
    accountId
    account {
      signingKey {
        privateKey {
          passwordProtected
          decrypted
        }
      }
    }
    memberOf {
      nodes {
        accountId: rowId
      }
    }
  }
  workflow: workflowByRowId(rowId: $workflowId) {
    config {
      id: rowId
    }
    groups {
      nodes {
        groupId: rowId
        accountId: ownerId
      }
    }
  }
}
