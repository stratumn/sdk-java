 mutation createLinkMutation($link: JSON!, $data: JSON) {
      createLink(input: { link: $link, data: $data }) {
        trace {
          ...TraceStateFragment
        }
      }
    }
    ${TraceStateFragment}